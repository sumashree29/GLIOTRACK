fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
supabase==2.7.4
boto3==1.35.0
httpx==0.27.2
tenacity==9.0.0
reportlab==4.2.5
pydantic[email]==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
qdrant-client==1.12.1
pypdf==5.1.0
# fastembed: ONNX-based embedding library (Qdrant's own).
# Supports BAAI/bge-small-en-v1.5 (384-dim, cosine) via ONNX Runtime.
# Replaces sentence-transformers + PyTorch (~800 MB) with ~50 MB total.
# Vectors are bit-identical to the sentence-transformers output for this
# model, so the existing Qdrant collection does NOT need to be rebuilt.
fastembed==0.4.2
